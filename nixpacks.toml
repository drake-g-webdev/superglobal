# Force Node.js provider for Next.js app
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.install]
cmds = ["npm install", "pip install -r api/requirements.txt"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
